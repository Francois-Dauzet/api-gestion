{% extends 'base.html.twig' %}

{% block title %}TFormats{% endblock %}

{% block body %}
    <h1>TFormats</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ t_format.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ t_format.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_formats_index') }}">back to list</a>

    <a href="{{ path('app_formats_edit', {'id': t_format.id}) }}">edit</a>

    {{ include('formats/_delete_form.html.twig') }}
{% endblock %}
