{% extends 'base.html.twig' %}

{% block title %}TNotes{% endblock %}

{% block body %}
    <h1>TNotes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ t_note.id }}</td>
            </tr>
            <tr>
                <th>CreatedDate</th>
                <td>{{ t_note.createdDate ? t_note.createdDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>NoteText</th>
                <td>{{ t_note.noteText }}</td>
            </tr>
            <tr>
                <th>AuthorName</th>
                <td>{{ t_note.authorName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_hives_notes_index') }}">back to list</a>

    <a href="{{ path('app_hives_notes_edit', {'id': t_note.id}) }}">edit</a>

    {{ include('hives_notes/_delete_form.html.twig') }}
{% endblock %}
