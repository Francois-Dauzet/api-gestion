<form method="post" action="{{ path('app_hives_notes_delete', {'id': t_note.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_note.id) }}">
    <button class="btn">Delete</button>
</form>
