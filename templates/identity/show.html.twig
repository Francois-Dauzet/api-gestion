{% extends 'base.html.twig' %}

{% block title %}TUsersInfoIdentity{% endblock %}

{% block body %}
    <h1>TUsersInfoIdentity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>FirstName</th>
                <td>{{ t_users_info_identity.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ t_users_info_identity.lastName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_identity_index') }}">back to list</a>

    <a href="{{ path('app_identity_edit', {'userIdIdentityAccount': t_users_info_identity.userIdIdentityAccount}) }}">edit</a>

    {{ include('identity/_delete_form.html.twig') }}
{% endblock %}
