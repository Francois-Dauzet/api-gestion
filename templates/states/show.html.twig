{% extends 'base.html.twig' %}

{% block title %}TStates{% endblock %}

{% block body %}
    <h1>TStates</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ t_state.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ t_state.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_states_index') }}">back to list</a>

    <a href="{{ path('app_states_edit', {'id': t_state.id}) }}">edit</a>

    {{ include('states/_delete_form.html.twig') }}
{% endblock %}
