{% extends 'base.html.twig' %} {% block title %}TUsersInfoAddress{% endblock %}
{% block body %}
<h1>TUsersInfoAddress</h1>

<table class="table">
  <tbody>
    <tr>
      <th>Numéro de rue</th>
      <td>{{ t_users_info_address.streetNumber }}</td>
    </tr>
    <tr>
      <th>Nom de rue</th>
      <td>{{ t_users_info_address.streetName }}</td>
    </tr>
    <tr>
      <th>Code postal</th>
      <td>{{ t_users_info_address.postalCode }}</td>
    </tr>
    <tr>
      <th>Ville</th>
      <td>{{ t_users_info_address.town }}</td>
    </tr>
  </tbody>
</table>

<a href="{{ path('app_address_index') }}">back to list</a>

<a
  href="{{
    path('app_address_edit', {
      userIdAddressAccount: t_users_info_address.userIdAddressAccount
    })
  }}"
  >edit</a
>

{{ include("address/_delete_form.html.twig") }}
{% endblock %}
