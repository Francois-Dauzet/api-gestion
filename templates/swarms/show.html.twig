{% extends 'base.html.twig' %}

{% block title %}TSwarms{% endblock %}

{% block body %}
    <h1>TSwarms</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ t_swarm.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_swarms_index') }}">back to list</a>

    <a href="{{ path('app_swarms_edit', {'id': t_swarm.id}) }}">edit</a>

    {{ include('swarms/_delete_form.html.twig') }}
{% endblock %}
