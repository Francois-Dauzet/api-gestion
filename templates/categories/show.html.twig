{% extends 'base.html.twig' %}

{% block title %}TCategories{% endblock %}

{% block body %}
    <h1>TCategories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ t_category.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ t_category.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categories_index') }}">back to list</a>

    <a href="{{ path('app_categories_edit', {'id': t_category.id}) }}">edit</a>

    {{ include('categories/_delete_form.html.twig') }}
{% endblock %}
