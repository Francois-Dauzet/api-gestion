<form method="post" action="{{ path('app_temperaments_delete', {'id': t_temperament.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_temperament.id) }}">
    <button class="btn">Delete</button>
</form>
