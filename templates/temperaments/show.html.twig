{% extends 'base.html.twig' %}

{% block title %}TTemperaments{% endblock %}

{% block body %}
    <h1>TTemperaments</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ t_temperament.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ t_temperament.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_temperaments_index') }}">back to list</a>

    <a href="{{ path('app_temperaments_edit', {'id': t_temperament.id}) }}">edit</a>

    {{ include('temperaments/_delete_form.html.twig') }}
{% endblock %}
