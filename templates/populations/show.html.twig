{% extends 'base.html.twig' %}

{% block title %}TPopulations{% endblock %}

{% block body %}
    <h1>TPopulations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ t_population.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ t_population.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_populations_index') }}">back to list</a>

    <a href="{{ path('app_populations_edit', {'id': t_population.id}) }}">edit</a>

    {{ include('populations/_delete_form.html.twig') }}
{% endblock %}
